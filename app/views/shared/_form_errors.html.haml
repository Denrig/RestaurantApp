- if local_assigns[:entity].errors.any?
  #error-explanation
    %h2 #{pluralize(local_assigns[:entity].errors.count, "error")} found while trying to save the product
    %ul
      - local_assigns[:entity].errors.full_messages.each do |message|
        %li= message
%h1 Forgot password
.row
.col-md-6.col-md-offset-3
= form_with(url: account_management_password_resets_path, scope: :password_reset,local: true) do |f|
  = f.label :email 
  = f.email_field :email, class: 'form-control'
  = f.submit "Submit", class: "btn btn-primary"
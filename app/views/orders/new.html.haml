= render 'shared/form_errors',entity: @order
= form_for @order,local: true,params:{cart: params[:cart]} do |f|
  = f.select :city, options_for_select(Order.cities.keys), class: ['selector'], prompt: 'City'
  = f.text_field :address
  = f.telephone_field :phone
  = f.submit 'Place Order'
   